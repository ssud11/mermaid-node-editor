name: CI

# Test + build only. Publishing is maintainer-only — this workflow NEVER runs `vsce publish`.
# Actions are SHA-pinned per the CI security baseline (tags can be force-pushed; SHAs cannot).

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 20
          cache: npm
      - name: Install (clean)
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Unit tests
        run: npm test
      - name: Package (.vsix build verification)
        run: npx @vscode/vsce package
      # IT-2: an integration job (xvfb-run + @vscode/test-electron) gets added here.
